# Tech debt tracker

## Open items

| Area                    | Debt                                                                                                                             | Why it matters                                                            | Suggested next step                                                                                                                                               |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Documentation ownership | Product specs partially backfilled: `F1-playlists-and-singer-rotation.md` and `queue-management.md` added during v0.9.           | Product behavior can drift when no single spec owns it.                   | Gradually add feature-level specs under `docs/product-specs/` for remaining areas (mic input, pitch shift, session recording). Track in [`./plan.md`](./plan.md). |
| Generated docs          | `docs/generated/db-schema.md` was manually refreshed. Now regenerated by `scripts/generate-db-schema.mjs`.                       | Generated summaries become stale if no refresh workflow exists.           | ✅ **Resolved** — see `pnpm generate:db-schema` and `scripts/README.md`.                                                                                          |
| Plan archival policy    | Active vs archived classification is hand-maintained.                                                                            | Useful plans can be lost or remain active too long.                       | ✅ **Resolved** — v0.9 plan archived to `docs/archive/plans/2026-05-13-v0.9-hardening-and-playlists-plan.md` per policy.                                          |
| Dead-code review        | This pass confirmed `src/lib/cover-art.ts` is still used, but there is no recurring dead-code sweep.                             | Small unused utilities accumulate and add noise for future work.          | Run periodic repo-wide usage checks during cleanup-oriented tasks.                                                                                                |
| Commercial OS signing   | macOS notarization + Windows Authenticode need **Apple Developer Program** and/or a paid code-signing cert for "quiet" installs. | Unsigned builds hit Gatekeeper / SmartScreen; users need honest guidance. | **Deferred** with rationale in [`../design-docs/releasing.md`](../design-docs/releasing.md); ship checksums and clear install copy instead.                       |
| Release checksums       | GitHub Release workflow now generates `SHA256SUMS` per H8.1.                                                                     | Without OS trust stores, checksums are the main free integrity signal.    | ✅ **Resolved** — `SHA256SUMS` generated in release workflow.                                                                                                     |
